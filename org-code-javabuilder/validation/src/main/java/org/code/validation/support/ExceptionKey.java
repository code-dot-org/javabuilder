package org.code.validation.support;

public enum ExceptionKey {
  // The user's main method could not be run
  ERROR_RUNNING_MAIN,
  // The user has no main method and we tried to run main
  NO_MAIN_METHOD
}
